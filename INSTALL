# INSTALL

## Install dependencies for build
sudo apt install libgcrypt-dev libstfl-dev zlib1g-dev libexpat1-dev

## Get repository (requires git)
git clone https://github.com/pepa65/kdbxviewer
cd kdbxviewer

## Install prerequisites
sudo apt install libgcrypt20-dev libexpat1-dev zlib1g-dev libstfl-dev

## Build the `kdbxviewer` binary in the `bin` directory
make

## Install the `kdbxviewer` binary to `/usr/local/bin`
sudo make install

## Uninstall: delete `/usr/local/bin/kdbxviewer`
sudo make uninstall
